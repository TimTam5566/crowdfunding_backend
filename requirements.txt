Django==5.1
djangorestframework==3.15.2
django-cors-headers==4.5.0 # sort of security related (cors = cross origin requestsharing) headers = bearer or auth token, limiting domain to serve appropriate content
dj-database-url==2.2.0
gunicorn==23.0.0 
psycopg2-binary==2.9.10
python-dotenv==1.0.1
whitenoise==6.7.0

# different libraries for running, or deploying on someone elses system, site.